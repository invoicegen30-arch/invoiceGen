import PricingClient from './pricingClient';

export const metadata = {
  title: 'Top-Up - Invoicerly',
  description: 'Top up tokens — pay-as-you-go, no subscriptions.',
};

export default function PricingPage() {
  return <PricingClient />;
}
